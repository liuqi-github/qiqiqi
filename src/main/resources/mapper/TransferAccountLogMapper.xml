<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.thread.demo.mapper.TransferAccountLogMapper">
    <select id="findLastTime" resultType="java.time.LocalDateTime">
        select max(end_time)
        from transfer_account_log
    </select>

</mapper>